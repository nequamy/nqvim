-- =============================================================================
-- lsp/buf_ls.lua — конфигурация Buf LSP для Protobuf
-- =============================================================================
-- buf — инструмент для работы с Protocol Buffers (.proto файлы).
-- Обеспечивает LSP поддержку: автодополнение, навигация, диагностика .proto файлов.
--
-- Установка: brew install bufbuild/buf/buf
-- Конфигурация: buf.yaml в корне проекта (схема, зависимости, lint правила)
-- =============================================================================

return {
	cmd = { 'buf', 'lsp', 'serve' }, -- запуск buf в режиме LSP сервера

	filetypes = { 'proto' }, -- активировать только для .proto файлов

	-- Маркеры корня проекта.
	-- buf.yaml — основной конфиг buf (обязателен для buf workspace).
	root_markers = { 'buf.yaml', '.git' }
}
