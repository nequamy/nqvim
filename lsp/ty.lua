-- =============================================================================
-- lsp/ty.lua — конфигурация Ty type checker для Python
-- =============================================================================
-- Ty — Python type checker от Astral, работающий параллельно с Ruff.
-- Он отвечает за типы, а Ruff — за стиль, линтинг и быстрые исправления.
-- =============================================================================

return {
	cmd = { "ty", "server" }, -- запуск Ty в режиме LSP-сервера
	filetypes = { "python" }, -- активировать только для .py файлов
	-- Маркеры вычисляются для каждого буфера: несколько Python-проектов не смешивают корни.
	root_markers = { "pyproject.toml", ".git" },
}
